fn unsigned(x: u32, y: u32) {
    x & y;
    x | y;
    x ^ y;
    x << y;
    x >> y;
}

fn signed(x: i32, y: i32) {
    x & y;
    x | y;
    x ^ y;
    x << y;
    x >> y;
}

fn bool(x: bool, y: bool) {
    x & y;
    x | y;
    x ^ y;
}
